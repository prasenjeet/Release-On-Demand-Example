apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service-account.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app: acme-api
  app.kubernetes.io/name: acme-api
  app.kubernetes.io/part-of: release-on-demand-demo
